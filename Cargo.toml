[package]
name = "plato-runtime"
version = "0.2.0"
edition = "2021"
description = "Self-discovering, self-optimizing compute runtime"
license = "MIT"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
